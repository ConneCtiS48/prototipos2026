version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 22
  pre_build:
    commands:
      - cd Nexus-T
      - npm install
  build:
    commands:
      - npm run build
artifacts:
  base-directory: Nexus-T/dist
  files:
    - '**/*'